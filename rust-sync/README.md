# Deprecated Location

Rust sources were moved to repository root.

Use:
- `Cargo.toml` at repo root
- `src/` at repo root

This directory remains only to preserve local editor artifacts (for example swap files).
